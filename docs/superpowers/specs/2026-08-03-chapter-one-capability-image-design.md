# 第一章能力架构图插入设计

## 目标

在《第 1 章 初识 WorkBuddy》的 Mermaid 任务流程图正下方增加用户提供的“WorkBuddy 完整能力架构图”，帮助读者从任务执行流程过渡到完整能力体系。

## 内容与位置

- 新图片紧跟 Mermaid 代码块之后展示。
- 保留后续“例如，用户可以直接告诉 WorkBuddy……”文案及现有销售数据/PPT 截图的位置。
- 不调整本章其他文案、图片和章节结构。

## 阅读指南文案

- 将阅读指南“WorkBuddy 新手入门路线”中的第一篇链接显示文案改为“第一篇：基础篇《从0到1：先把 WorkBuddy 用起来》”。
- 将阅读指南“WorkBuddy 团队落地路线”中的第三篇链接显示文案改为“第三篇：进阶篇《把案例变成可复用的工作系统》”。
- 将阅读指南“WorkBuddy 团队落地路线”中的第四篇链接显示文案改为“第四篇：实战篇《落到岗位与行业，组建AI团队》”。
- 保留三个链接当前目标地址，不修改路由。
- 保留链接前后的“从……开始，依次完成：”句式及下方步骤列表。
- 保留“重点阅读第三篇……与第四篇……”句式及团队实践列表。

## 素材管理

- 将用户提供的 JPG 复制到 `docs/public/article-assets/source-calibration/ch01/004.jpg`。
- Markdown 使用站点绝对路径 `/article-assets/source-calibration/ch01/004.jpg`，不引用用户下载目录。
- 图片 alt 文本使用“WorkBuddy 完整能力架构图”。

## 展示规则

- 沿用 VitePress 正文图片的默认响应式行为。
- 桌面端在正文内容宽度内完整展示，不增加边框或额外容器。
- 移动端宽度自适应为可用内容宽度，不产生横向滚动。
- 不增加图注、灯箱或点击放大功能。

## 首页资料区标题响应式规则

- “获取 WorkBuddy 小白书与配套资料”在宽度大于 `1200px` 的桌面视口保持单行展示。
- 在宽度不超过 `1200px` 的中等及更小视口解除强制不换行，允许标题按可用空间自然排成两行。
- 保持现有标题字号、字重和内容，不通过缩小文字规避裁切。
- 保持资料区其余文字、按钮和 IP 图片布局规则不变。
- 手机端继续沿用现有单列布局。

## 验证

- 回归测试确认第一章 Markdown 在 Mermaid 代码块之后引用新图片及准确 alt 文本。
- 确认素材文件存在且由站点构建收录。
- 确认阅读指南包含新的第一篇、第三篇和第四篇链接文案，并继续分别指向原有目录地址。
- 在约 `823px` 宽的视口验证资料区标题完整显示、可换为两行且页面无横向溢出。
- 验证宽度大于 `1200px` 时资料区标题仍保持单行。
- 运行完整测试和生产构建。
